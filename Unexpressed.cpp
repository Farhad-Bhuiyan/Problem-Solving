#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n, x;
    cin >> n >> x;

    vector<long long> v(n + 1);
    for (long long i = 1; i <= n; i++)
    {
        cin >> v[i];
    }
    bool l = false;
    for (long long i = 1; i <= n; i++)
    {
    }

    return 0;
}
