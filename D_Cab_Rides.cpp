#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        int to=0;
        if(n<=2)cout << 200 << endl;
        else
        {
            cout << n*100 << endl;
        }
    }
    return 0;
}